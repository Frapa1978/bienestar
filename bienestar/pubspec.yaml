name: bienestar
description: A new Flutter project.

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.0
  fl_chart: ^0.69.0
  csv: ^6.0.0
  path_provider: ^2.1.0
  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    # - assets/images/   # opcional, si luego usas esta carpeta
